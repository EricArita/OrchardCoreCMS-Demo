@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@using FuturifyModule.Models
@model IEnumerable<ApiProjectModel>

<div style="margin-top:50px">
    <div>
        <a asp-action="Index" asp-controller="Leader">
            Leader List
        </a>
    </div>

    <div>
        <a asp-action="CreateNewProject" asp-controller="Project" class="btn btn-success" style="margin-top: 10px">
            Create new project
        </a>
    </div>

    <div class="info" style="padding-top:30px">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <td>Name</td>
                    <td>LeadId</td>
                    <td></td>
                </tr>
            </thead>
            <tbody>
                @{
                    if (Model != null && Model.Count() != 0)
                    {
                        foreach (var project in Model)
                        {
                            <tr>
                                <td>@project.Name</td>
                                <td>@project.LeaderId</td>
                                <td>
                                    <a href="/EditProject/@project.Id">Edit</a> |
                                    <a href="/DeleteProject/@project.Id">Delete</a>
                                </td>
                            </tr>
                        }
                    }
                }

            </tbody>
        </table>
    </div>
</div>
